// Test simplified vibe syntax

// Simple variable assignment
let x = "hello"
const y = "world"

// Function that returns a string
function greet(name) {
  return "Hello, {name}!"
}

// Call function
let msg = greet("Vibe")

// AI task
model m = { name: "gpt-4", apiKey: "test", url: "http://test" }
let answer = vibe "what is the meaning of life?" m default

// Conditional with truthy string
if msg {
  let inner = vibe "explain {msg}" m default
}

// Return final value
msg
